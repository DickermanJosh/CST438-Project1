import * as React from 'react';

// TODO: Make this after ensuring the DB itself works
/*
describe('UserDAO Tests', () => {
    // Create user table before starting tests
    beforeAll(() => {
        // UserDAO.createUserTable();
    });
});

// Insert user test
it(`Should insert a user into the db`, done => {
    // UserDAO.insertUser('John', '1234'); // username 'John' password '1234'

    // UserDAO.getAllUsers
});*/
